---
import { SITE } from '@/config';
---

<footer class="py-12 mt-24 sm:mt-32 lg:mt-48 border-t border-border">
  <div class="flex flex-col md:flex-row justify-between items-center gap-8 sm:gap-6">
    <div class="flex items-center gap-3 text-xs font-bold text-muted-foreground uppercase tracking-[0.2em] opacity-60">
      <span>&copy; {new Date().getFullYear()} {SITE.author}</span>
      <span aria-hidden="true">&middot;</span>
      <a href="/privacy-policy" class="hover:text-primary hover:opacity-100 transition-all no-underline">
        Privacy Policy
      </a>
    </div>

    <div class="flex flex-wrap justify-center gap-6 sm:gap-8">
      {
        Object.entries(SITE.social).map(([name, url]) => (
          <a
            href={url}
            target="_blank"
            rel="noopener noreferrer"
            aria-label={`Visit our ${name} page`}
            class="text-xs font-bold uppercase tracking-widest text-muted-foreground hover:text-primary transition-all no-underline"
          >
            {name}
          </a>
        ))
      }
    </div>
  </div>
</footer>
