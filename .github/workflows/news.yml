name: 'Weekly News Brief - Phase 1-2 (Gemini Synthesis + Dual-Post Output)'

on:
  schedule:
    - cron: '0 8 * * MON'  # Every Monday at 08:00 UTC
  workflow_dispatch:

permissions:
  contents: write  # Required to push to gh-pages

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout main branch
        uses: actions/checkout@v3
        with:
          fetch-depth: 0        # get full history so we can compare/merge
          persist-credentials: true  # allow authenticated push using GITHUB_TOKEN

      - name: Set up Ruby for Jekyll
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: 3.1
          bundler-cache: true

      - name: Install Ruby dependencies
        run: |
          gem install jekyll bundler
          bundle install || true

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Install Python dependencies
        run: pip install -r requirements.txt || true

      - name: Run fetch_news.py to generate markdown posts
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: python fetch_news.py

      - name: Clean build artifacts before branch switching
        run: |
          rm -rf _site Gemfile.lock .jekyll-cache .jekyll-metadata || true
          git clean -fdx -- Gemfile.lock _site .jekyll-cache .jekyll-metadata || true

      - name: Build Jekyll site with Chirpy theme
        run: |
          bundle exec jekyll build --source . --destination _site

      - name: Deploy to gh-pages branch
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"

          # Clean untracked files that would block branch switching
          rm -rf Gemfile.lock _site .jekyll-cache .jekyll-metadata || true
          git clean -fdx -- Gemfile.lock _site .jekyll-cache .jekyll-metadata || true
          git reset --hard || true

          # Save the built site, posts, config before switching branches
          mkdir -p /tmp/gh_pages_deploy
          cp -r _site/* /tmp/gh_pages_deploy/ 2>/dev/null || true
          mkdir -p /tmp/gh_pages_deploy/_posts
          cp _posts/*.md /tmp/gh_pages_deploy/_posts/ 2>/dev/null || true
          cp _config.yml /tmp/gh_pages_deploy/ 2>/dev/null || true
          cp Gemfile /tmp/gh_pages_deploy/ 2>/dev/null || true

          # Switch to gh-pages branch
          git fetch origin gh-pages || true
          if git show-ref --verify --quiet refs/remotes/origin/gh-pages; then
            git checkout gh-pages
            git pull origin gh-pages --allow-unrelated-histories || true
          else
            git checkout --orphan gh-pages
          fi

          # Clean everything except .git, prepare for new content
          git rm -rf . --cached --force 2>/dev/null || true
          rm -rf * 2>/dev/null || true

          # Restore built site and configuration
          cp -r /tmp/gh_pages_deploy/* . 2>/dev/null || true

          # Ensure _posts directory exists with posts
          mkdir -p _posts
          if [ -d /tmp/gh_pages_deploy/_posts ]; then
            cp /tmp/gh_pages_deploy/_posts/*.md _posts/ 2>/dev/null || true
          fi

          # Commit if there are changes
          git add -A || true
          if ! git diff --cached --quiet; then
            git commit -m "chore: publish site with Chirpy theme ($(date +%Y-%m-%d\ %H:%M:%S))"
            git push origin gh-pages --force-with-lease
          else
            echo "No changes to deploy"
          fi