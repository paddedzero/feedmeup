name: 'Weekly News Brief - Phase 1-2 (Gemini Synthesis + Dual-Post Output)'

on:
  schedule:
    - cron: '0 8 * * MON'  # Every Monday at 08:00 UTC
  workflow_dispatch:

permissions:
  contents: write
  pages: write
  id-token: write

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout main branch
        uses: actions/checkout@v3
        with:
          fetch-depth: 0
          persist-credentials: true

      - name: Set up Ruby for Jekyll
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: 3.2
          bundler-cache: true

      - name: Install Ruby dependencies
        run: bundle install

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.x'

      - name: Install Python dependencies
        run: pip install -r requirements.txt

      - name: Run fetch_news.py to generate markdown posts
        env:
          GEMINI_API_KEY: ${{ secrets.GEMINI_API_KEY }}
        run: python fetch_news.py

      - name: Remove conflicting custom styles to allow Chirpy theme CSS to load
        run: |
          # Remove custom CSS that overrides Chirpy theme
          rm -f assets/css/style.css || true
          echo "Removed custom style.css to use Chirpy theme CSS"

      - name: Build site with Jekyll
        run: bundle exec jekyll build

      - name: "🔵 BLUE: Verify user content integrity before deployment"
        run: |
          echo "Checking that all user content is present..."
          bash scripts/verify-content-safety.sh
          echo "✅ User content verified - safe to proceed with deployment"

      - name: "🟢 GREEN: Build and test in staging area"
        run: |
          # Create a staging directory to test the build
          mkdir -p /tmp/staging
          cp -r ./_site/* /tmp/staging/ 2>/dev/null || true
          
          # Verify critical files exist in staging
          if [ ! -f "/tmp/staging/index.html" ]; then
            echo "❌ ERROR: index.html not built in staging!"
            exit 1
          fi
          
          if [ ! -f "/tmp/staging/feed.xml" ]; then
            echo "⚠️  WARNING: feed.xml not found in staging"
          fi
          
          echo "✅ Staging build validated - ready for production deployment"
          ls -lh /tmp/staging/index.html /tmp/staging/feed.xml 2>/dev/null || true

      - name: Deploy to gh-pages branch
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          # Capture artifacts from main branch BEFORE switching branches.
          # Otherwise, `_site` and newly-generated `_posts` are lost after checkout.
          echo "📦 Capturing build artifacts from main..."
          SITE_BUILD_DIR=/tmp/site_build
          POSTS_BUILD_DIR=/tmp/generated_posts
          rm -rf "$SITE_BUILD_DIR" "$POSTS_BUILD_DIR" || true
          mkdir -p "$SITE_BUILD_DIR" "$POSTS_BUILD_DIR" || true

          if [ ! -d "_site" ]; then
            echo "❌ ERROR: _site not found; Jekyll build likely failed"
            exit 1
          fi
          cp -r ./_site/* "$SITE_BUILD_DIR/" 2>/dev/null || true

          if [ -d "_posts" ]; then
            cp -r ./_posts "$POSTS_BUILD_DIR/" || true
          fi

          # BLUE-GREEN DEPLOYMENT
          # 1. Prepare "green" (new) environment
          # 2. Test it completely
          # 3. Only then remove "blue" (old) and promote green
          
          # Clean up any stray Gemfile.lock and untracked files before branch switching
          rm -f Gemfile.lock || true
          git clean -fd || true
          
          # Configure git
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          
          # Fetch gh-pages branch (current production)
          git fetch origin gh-pages || true
          
          # Switch to gh-pages, rebasing on remote
          git checkout -B gh-pages origin/gh-pages || git checkout --orphan gh-pages
          
          # ===== BACKUP PHASE (BLUE) =====
          echo "📦 Backing up current production..."
          BACKUP_TIME=$(date +%s)
          mkdir -p /tmp/blue_backup_$BACKUP_TIME
          cp -r . /tmp/blue_backup_$BACKUP_TIME/ || true
          echo "✅ Production backed up to: /tmp/blue_backup_$BACKUP_TIME"
          
          # ===== PREPARE GREEN DEPLOYMENT =====
          echo "🟢 Preparing green deployment..."
          
          # Save existing posts (ALWAYS backup user content)
          mkdir -p /tmp/posts_backup
          if [ -d "_posts" ]; then
            cp -r _posts /tmp/posts_backup/ || true
          fi
          POSTS_BEFORE=$(find _posts -name "*.md" 2>/dev/null | wc -l || echo 0)
          echo "Posts before deployment: $POSTS_BEFORE"

          # Replace gh-pages contents with Jekyll SOURCE FILES (not pre-built HTML)
          # GitHub Pages will build Jekyll server-side with Chirpy theme
          # Keep .git intact.
          echo "🧹 Clearing existing gh-pages working tree..."
          find . -mindepth 1 -maxdepth 1 -not -name ".git" -exec rm -rf {} + || true

          echo "📦 Deploying Jekyll source files to gh-pages..."
          # Deploy essential Jekyll source files for GitHub Pages to build
          git checkout main -- _config.yml Gemfile Gemfile.lock || true
          git checkout main -- _tabs || true
          git checkout main -- _data || true
          git checkout main -- assets || true
          git checkout main -- _includes || true
          git checkout main -- _layouts || true
          git checkout main -- index.md || true
          git checkout main -- .gitignore || true
          
          # Deploy generated posts
          if [ -d "$POSTS_BUILD_DIR/_posts" ]; then
            mkdir -p _posts
            cp -r "$POSTS_BUILD_DIR/_posts"/* ./_posts/ || true
          fi
          
          # REMOVE .nojekyll to allow GitHub Pages to rebuild Jekyll with Chirpy theme
          rm -f .nojekyll || true
          
          # ===== TEST GREEN DEPLOYMENT =====
          echo "🧪 Testing green deployment..."
          
          # Verify critical Jekyll source files exist
          if [ ! -f "_config.yml" ]; then
            echo "❌ CRITICAL: _config.yml missing in green deployment!"
            echo "Rolling back to blue (production)..."
            cp -r /tmp/blue_backup_$BACKUP_TIME/* . || true
            git add -A
            git commit -m "rollback: Green deployment failed - _config.yml missing" || true
            git push -u origin gh-pages
            exit 1
          fi
          
          # Verify _tabs directory exists
          if [ ! -d "_tabs" ]; then
            echo "⚠️  WARNING: _tabs directory missing (sidebar tabs won't render)"
          fi
          
          # Verify posts count didn't decrease
          POSTS_AFTER=$(find _posts -name "*.md" 2>/dev/null | wc -l || echo 0)
          echo "Posts after deployment: $POSTS_AFTER"
          if [ "$POSTS_AFTER" -lt "$POSTS_BEFORE" ]; then
            echo "❌ CRITICAL: Posts were deleted! ($POSTS_BEFORE → $POSTS_AFTER)"
            echo "Rolling back to blue..."
            cp -r /tmp/blue_backup_$BACKUP_TIME/* . || true
            git add -A
            git commit -m "rollback: Post count decreased" || true
            git push -u origin gh-pages
            exit 1
          fi
          
          echo "✅ Green deployment validated"
          
          # ===== PROMOTE GREEN (remove old blue) =====
          echo "🟢 Promoting green to production..."
          
          # Stage all changes
          git add -A
          
          # Commit if there are changes
          if ! git diff --cached --quiet; then
            git commit -m "publish: Jekyll build $(date +%Y-%m-%d\ %H:%M:%S) - blue-green deployment verified"
            git push -u origin gh-pages
            echo "✅ Green promoted to production"
            echo "📊 Deployment stats: Posts=$POSTS_AFTER, Timestamp=$(date)"
          else
            echo "ℹ️  No changes to deploy"
          fi
          
          # Cleanup old backups (keep only last 3)
          echo "🧹 Cleaning up old backups..."
          ls -1d /tmp/blue_backup_* 2>/dev/null | sort -r | tail -n +4 | xargs rm -rf 2>/dev/null || true